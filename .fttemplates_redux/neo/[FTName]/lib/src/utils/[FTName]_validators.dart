class <FTName | pascalcase>Validators {
  // example
  // static RegExp get birthDayRegExp => RegExp(r'^(\d){2}\.(\d){2}\.(\d){4}$');

  // String? phoneValidator(String? value, BuildContext context) {
  //   if (value == null) {
  //     return null;
  //   }

  //   final bool includeCountyCode = value.startsWith('+7 ');

  //   final PhoneInputFormatter phoneFormatter = PhoneInputFormatter(includeCountyCode: includeCountyCode);
  //   phoneFormatter.maskedText(value);

  //   if (!phoneFormatter.isFill()) {
  //     return RegistrationIntl.of(context).typeCorrectnPhoneNumber;
  //   }

  //   return null;
  // }
}
